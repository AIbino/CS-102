#include <stdio.h> // Includes the specified library

int main(void) { // The main function; required for all C programs to run
  printf("Hello World!"); // Printf is derived from stdio.h; obviously prints out "Hello World!"
  return 0; // Exit code for the program to make sure it actually worked
} //The grand finale; a closed bracket!
